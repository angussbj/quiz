import type { QuizDataRow } from './QuizDataRow';

/** Parse CSV text into quiz data rows. Placeholder. */
export function loadQuizData<K extends string>(_csvText: string): ReadonlyArray<QuizDataRow<K>> {
  return [];
}
