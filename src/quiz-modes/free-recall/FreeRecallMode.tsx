import type { QuizModeProps } from '../QuizModeProps';

/** Free recall mode (ordered and unordered). Placeholder. */
export function FreeRecallMode(_props: QuizModeProps) {
  return <div>Free Recall Mode</div>;
}
