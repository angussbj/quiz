import type { QuizModeProps } from '../QuizModeProps';

/** Identify mode: click the correct element. Placeholder. */
export function IdentifyMode(_props: QuizModeProps) {
  return <div>Identify Mode</div>;
}
