interface TimerProps {
  readonly elapsedMs: number;
  readonly running: boolean;
}

/** Optional quiz timer display. Placeholder. */
export function Timer(_props: TimerProps) {
  return <div>Timer</div>;
}
