import type { QuizModeProps } from '../QuizModeProps';

/** Locate mode: click as close as possible to the target. Placeholder. */
export function LocateMode(_props: QuizModeProps) {
  return <div>Locate Mode</div>;
}
